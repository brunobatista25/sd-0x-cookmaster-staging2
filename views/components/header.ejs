<header class="box box90 flex-row">
  <h1>Cookmaster</h1>
  <h1>Receitas</h1>
  <% if (name) {%>
  <h2><%= name %></h2>
  <% } %>
  <a class="box btn btn-warning" data-testid="login" onclick="window.location.pathname='/<%= endpoint %>'">
    <%= endpoint %>
  </a>
</header>
